import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-oidc-login',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
